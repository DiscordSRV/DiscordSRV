# Language setting
language: java
jdk:
- oraclejdk8

build:
  pre_ci:
    - echo "maven is hard"

  ci:
    - mvn compile
    - mvn package

  post_ci:
    - du -h target/DiscordSRV*.jar
    - ls -a
    - pwd
